name: gkm_gardener
description: Ghar Ka Mali — Gardener App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  lottie: ^3.3.2
  http: ^1.2.0
  shared_preferences: ^2.3.0
  provider: ^6.1.2
  go_router: ^13.2.0
  google_fonts: ^6.2.1
  fl_chart: ^0.68.0
  intl: ^0.19.0
  image_picker: ^1.1.0
  geolocator: ^12.0.0
  url_launcher: ^6.3.0
  flutter_animate: ^4.5.0
  shimmer: ^3.0.0
  cached_network_image: ^3.3.1
  pin_code_fields: ^8.0.1
  flutter_svg: ^2.0.10+1
  cupertino_icons: ^1.0.8
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

